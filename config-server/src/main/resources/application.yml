server:
  port: 8888
spring:
  cloud:
    config:
      server:
        encrypt.enabled: true
        git:
          uri: https://github.com/doruk581/config-repository/
          searchPaths: creditapproveservice